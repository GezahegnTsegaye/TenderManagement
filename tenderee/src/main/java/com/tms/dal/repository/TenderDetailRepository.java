package com.tms.dal.repository;

import com.tms.dal.model.TenderDetail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TenderDetailRepository extends JpaRepository<TenderDetail, Long> {
}
