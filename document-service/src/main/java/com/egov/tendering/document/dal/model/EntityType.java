package com.egov.tendering.document.dal.model;

/**
 * Enum representing the type of entity the document is associated with
 */
public enum EntityType {
    TENDER,
    BID,
    CONTRACT,
    USER,
    VENDOR,
    GOVERNMENT_AGENCY
}
